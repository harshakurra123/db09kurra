extends layout

block content
  h1= title
  div(class="container-fluid")
  div(class="row m-1")
  each result in results
    - let destination = `dispatch(\"/resource/update?id=${result._id}\")`
    div(class="row-sm-2", class="m-1") Item #{result.color}(#{result.weight}) is #{result.vitamins}
    button.btn( class="btn-primary ml-1", onclick= destination) Update

  script.
    function dispatch(path){
      window.location.href=path
    }
  //- div(class="container-fluid")
  //-     div(class="row")
  //-         -
  //-           let vegetables = results
  //-         each vegetable in vegetables
  //-           div(class="col-sm-2") #{vegetable.color}
  //-           div(class="col-sm-2") #{vegetable.weight}
  //-           div(class="col-sm-2") #{vegetable.vitamins}
